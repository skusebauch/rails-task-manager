<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <% if task.completed %>
        ☑️ <%= task.title %>
      <% else %>
        🤯 <%= task.title %>:
      <% end %>
      <%= task.details %>
    </li>
  <% end %>
</ul>
